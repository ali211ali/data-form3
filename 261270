<!DOCTYPE html>
<html lang="ar">
<head>
<meta charset="UTF-8">
<title>نموذج بيانات متعدد الخطوات</title>
<style>
  body {
    font-family: "Tahoma", sans-serif;
    background: #f5f5f5;
    direction: rtl;
    text-align: center;
    padding: 40px;
  }
  .form-step {
    background: white;
    padding: 20px;
    border-radius: 10px;
    display: none;
    box-shadow: 0 0 10px #ccc;
    max-width: 400px;
    margin: auto;
  }
  .form-step.active {
    display: block;
  }
  input {
    width: 90%;
    padding: 10px;
    margin: 10px 0;
    border-radius: 6px;
    border: 1px solid #ccc;
  }
  button {
    background: #0099ff;
    color: white;
    border: none;
    padding: 10px 20px;
    border-radius: 6px;
    cursor: pointer;
    margin-top: 10px;
  }
</style>
</head>
<body>

<h2>الرجاء إدخال بياناتك</h2>

<form id="multiStepForm">
  <!-- خطوة 1 -->
  <div class="form-step active">
    <label>الاسم:</label><br>
    <input type="text" id="name" required><br>
    <button type="button" onclick="nextStep()">التالي</button>
  </div>

  <!-- خطوة 2 -->
  <div class="form-step">
    <label>العمر:</label><br>
    <input type="number" id="age" required><br>
    <button type="button" onclick="nextStep()">التالي</button>
  </div>

  <!-- خطوة 3 -->
  <div class="form-step">
    <label>البريد الإلكتروني:</label><br>
    <input type="email" id="email" required><br>
    <button type="submit">إرسال</button>
  </div>
</form>

<script>
const BOT_TOKEN = "YOUR_TOKEN"; // ضع توكن البوت هنا
const CHAT_ID = "YOUR_CHAT_ID"; // ضع chat_id هنا
const API_URL = `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`;

let currentStep = 0;
const steps = document.querySelectorAll(".form-step");

function nextStep() {
  const inputs = steps[currentStep].querySelectorAll("input");
  for (let input of inputs) {
    if (!input.checkValidity()) {
      input.reportValidity();
      return;
    }
  }
  steps[currentStep].classList.remove("active");
  currentStep++;
  steps[currentStep].classList.add("active");
}

document.getElementById("multiStepForm").addEventListener("submit", function(e) {
  e.preventDefault();
  const name = document.getElementById("name").value;
  const age = document.getElementById("age").value;
  const email = document.getElementById("email").value;
  
  const message = `📩 تم استلام نموذج جديد:\n\n👤 الاسم: ${name}\n🎂 العمر: ${age}\n✉️ البريد: ${email}`;
  
  fetch(API_URL, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ chat_id: CHAT_ID, text: message })
  }).then(() => {
    document.body.innerHTML = `
      <h2>✅ شكراً لك!</h2>
      <p>تم إرسال بياناتك بنجاح.</p>
    `;
  }).catch(() => {
    document.body.innerHTML = `
      <h2>❌ حدث خطأ!</h2>
      <p>لم نتمكن من إرسال بياناتك، يرجى المحاولة لاحقًا.</p>
    `;
  });
});
</script>

</body>
</html>